name: erb_infinite_scroll
description:
  ERb libs - Riverpod implementation of infinite_scroll_pagination, this package
  allows to use infinite_scroll_pagination with Riverpod abstracting away the
  complexity
version: 0.0.4
homepage: https://github.com/nguyenhan-se/erb_flutter_boilerplate

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  infinite_scroll_pagination: ^4.0.0
  collection: ^1.18.0
  skeletonizer: 1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  riverpod_annotation: ^2.3.5
  riverpod_generator: ^2.4.0

flutter:
